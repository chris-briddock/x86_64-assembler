section .text
global _start
_start:
    call fn_000
    call fn_001
    call fn_002
    call fn_003
    call fn_004
    call fn_005
    call fn_006
    call fn_007
    call fn_008
    call fn_009
    call fn_010
    call fn_011
    call fn_012
    call fn_013
    call fn_014
    call fn_015
    call fn_016
    call fn_017
    call fn_018
    call fn_019
    call fn_020
    call fn_021
    call fn_022
    call fn_023
    call fn_024
    call fn_025
    call fn_026
    call fn_027
    call fn_028
    call fn_029
    call fn_030
    call fn_031
    call fn_032
    call fn_033
    call fn_034
    call fn_035
    call fn_036
    call fn_037
    call fn_038
    call fn_039
    call fn_040
    call fn_041
    call fn_042
    call fn_043
    call fn_044
    call fn_045
    call fn_046
    call fn_047
    call fn_048
    call fn_049
    call fn_050
    call fn_051
    call fn_052
    call fn_053
    call fn_054
    call fn_055
    call fn_056
    call fn_057
    call fn_058
    call fn_059
    call fn_060
    call fn_061
    call fn_062
    call fn_063
    call fn_064
    call fn_065
    call fn_066
    call fn_067
    call fn_068
    call fn_069
    call fn_070
    call fn_071
    call fn_072
    call fn_073
    call fn_074
    call fn_075
    call fn_076
    call fn_077
    call fn_078
    call fn_079
    call fn_080
    call fn_081
    call fn_082
    call fn_083
    call fn_084
    call fn_085
    call fn_086
    call fn_087
    call fn_088
    call fn_089
    call fn_090
    call fn_091
    call fn_092
    call fn_093
    call fn_094
    call fn_095
    call fn_096
    call fn_097
    call fn_098
    call fn_099
    mov rax, $60
    xor rdi, rdi
    syscall

fn_000:
    push rbp
    mov rbp, rsp
    mov rax, $0
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_001:
    push rbp
    mov rbp, rsp
    mov rax, $1
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_002:
    push rbp
    mov rbp, rsp
    mov rax, $2
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_003:
    push rbp
    mov rbp, rsp
    mov rax, $3
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_004:
    push rbp
    mov rbp, rsp
    mov rax, $4
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_005:
    push rbp
    mov rbp, rsp
    mov rax, $5
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_006:
    push rbp
    mov rbp, rsp
    mov rax, $6
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_007:
    push rbp
    mov rbp, rsp
    mov rax, $7
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_008:
    push rbp
    mov rbp, rsp
    mov rax, $8
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_009:
    push rbp
    mov rbp, rsp
    mov rax, $9
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_010:
    push rbp
    mov rbp, rsp
    mov rax, $10
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_011:
    push rbp
    mov rbp, rsp
    mov rax, $11
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_012:
    push rbp
    mov rbp, rsp
    mov rax, $12
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_013:
    push rbp
    mov rbp, rsp
    mov rax, $13
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_014:
    push rbp
    mov rbp, rsp
    mov rax, $14
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_015:
    push rbp
    mov rbp, rsp
    mov rax, $15
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_016:
    push rbp
    mov rbp, rsp
    mov rax, $16
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_017:
    push rbp
    mov rbp, rsp
    mov rax, $17
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_018:
    push rbp
    mov rbp, rsp
    mov rax, $18
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_019:
    push rbp
    mov rbp, rsp
    mov rax, $19
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_020:
    push rbp
    mov rbp, rsp
    mov rax, $20
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_021:
    push rbp
    mov rbp, rsp
    mov rax, $21
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_022:
    push rbp
    mov rbp, rsp
    mov rax, $22
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_023:
    push rbp
    mov rbp, rsp
    mov rax, $23
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_024:
    push rbp
    mov rbp, rsp
    mov rax, $24
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_025:
    push rbp
    mov rbp, rsp
    mov rax, $25
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_026:
    push rbp
    mov rbp, rsp
    mov rax, $26
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_027:
    push rbp
    mov rbp, rsp
    mov rax, $27
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_028:
    push rbp
    mov rbp, rsp
    mov rax, $28
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_029:
    push rbp
    mov rbp, rsp
    mov rax, $29
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_030:
    push rbp
    mov rbp, rsp
    mov rax, $30
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_031:
    push rbp
    mov rbp, rsp
    mov rax, $31
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_032:
    push rbp
    mov rbp, rsp
    mov rax, $32
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_033:
    push rbp
    mov rbp, rsp
    mov rax, $33
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_034:
    push rbp
    mov rbp, rsp
    mov rax, $34
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_035:
    push rbp
    mov rbp, rsp
    mov rax, $35
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_036:
    push rbp
    mov rbp, rsp
    mov rax, $36
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_037:
    push rbp
    mov rbp, rsp
    mov rax, $37
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_038:
    push rbp
    mov rbp, rsp
    mov rax, $38
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_039:
    push rbp
    mov rbp, rsp
    mov rax, $39
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_040:
    push rbp
    mov rbp, rsp
    mov rax, $40
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_041:
    push rbp
    mov rbp, rsp
    mov rax, $41
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_042:
    push rbp
    mov rbp, rsp
    mov rax, $42
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_043:
    push rbp
    mov rbp, rsp
    mov rax, $43
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_044:
    push rbp
    mov rbp, rsp
    mov rax, $44
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_045:
    push rbp
    mov rbp, rsp
    mov rax, $45
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_046:
    push rbp
    mov rbp, rsp
    mov rax, $46
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_047:
    push rbp
    mov rbp, rsp
    mov rax, $47
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_048:
    push rbp
    mov rbp, rsp
    mov rax, $48
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_049:
    push rbp
    mov rbp, rsp
    mov rax, $49
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_050:
    push rbp
    mov rbp, rsp
    mov rax, $50
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_051:
    push rbp
    mov rbp, rsp
    mov rax, $51
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_052:
    push rbp
    mov rbp, rsp
    mov rax, $52
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_053:
    push rbp
    mov rbp, rsp
    mov rax, $53
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_054:
    push rbp
    mov rbp, rsp
    mov rax, $54
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_055:
    push rbp
    mov rbp, rsp
    mov rax, $55
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_056:
    push rbp
    mov rbp, rsp
    mov rax, $56
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_057:
    push rbp
    mov rbp, rsp
    mov rax, $57
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_058:
    push rbp
    mov rbp, rsp
    mov rax, $58
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_059:
    push rbp
    mov rbp, rsp
    mov rax, $59
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_060:
    push rbp
    mov rbp, rsp
    mov rax, $60
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_061:
    push rbp
    mov rbp, rsp
    mov rax, $61
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_062:
    push rbp
    mov rbp, rsp
    mov rax, $62
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_063:
    push rbp
    mov rbp, rsp
    mov rax, $63
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_064:
    push rbp
    mov rbp, rsp
    mov rax, $64
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_065:
    push rbp
    mov rbp, rsp
    mov rax, $65
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_066:
    push rbp
    mov rbp, rsp
    mov rax, $66
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_067:
    push rbp
    mov rbp, rsp
    mov rax, $67
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_068:
    push rbp
    mov rbp, rsp
    mov rax, $68
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_069:
    push rbp
    mov rbp, rsp
    mov rax, $69
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_070:
    push rbp
    mov rbp, rsp
    mov rax, $70
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_071:
    push rbp
    mov rbp, rsp
    mov rax, $71
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_072:
    push rbp
    mov rbp, rsp
    mov rax, $72
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_073:
    push rbp
    mov rbp, rsp
    mov rax, $73
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_074:
    push rbp
    mov rbp, rsp
    mov rax, $74
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_075:
    push rbp
    mov rbp, rsp
    mov rax, $75
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_076:
    push rbp
    mov rbp, rsp
    mov rax, $76
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_077:
    push rbp
    mov rbp, rsp
    mov rax, $77
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_078:
    push rbp
    mov rbp, rsp
    mov rax, $78
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_079:
    push rbp
    mov rbp, rsp
    mov rax, $79
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_080:
    push rbp
    mov rbp, rsp
    mov rax, $80
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_081:
    push rbp
    mov rbp, rsp
    mov rax, $81
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_082:
    push rbp
    mov rbp, rsp
    mov rax, $82
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_083:
    push rbp
    mov rbp, rsp
    mov rax, $83
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_084:
    push rbp
    mov rbp, rsp
    mov rax, $84
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_085:
    push rbp
    mov rbp, rsp
    mov rax, $85
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_086:
    push rbp
    mov rbp, rsp
    mov rax, $86
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_087:
    push rbp
    mov rbp, rsp
    mov rax, $87
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_088:
    push rbp
    mov rbp, rsp
    mov rax, $88
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_089:
    push rbp
    mov rbp, rsp
    mov rax, $89
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_090:
    push rbp
    mov rbp, rsp
    mov rax, $90
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_091:
    push rbp
    mov rbp, rsp
    mov rax, $91
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_092:
    push rbp
    mov rbp, rsp
    mov rax, $92
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_093:
    push rbp
    mov rbp, rsp
    mov rax, $93
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_094:
    push rbp
    mov rbp, rsp
    mov rax, $94
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_095:
    push rbp
    mov rbp, rsp
    mov rax, $95
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_096:
    push rbp
    mov rbp, rsp
    mov rax, $96
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_097:
    push rbp
    mov rbp, rsp
    mov rax, $97
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_098:
    push rbp
    mov rbp, rsp
    mov rax, $98
    add rax, $1
    xor rax, rax
    pop rbp
    ret

fn_099:
    push rbp
    mov rbp, rsp
    mov rax, $99
    add rax, $1
    xor rax, rax
    pop rbp
    ret

